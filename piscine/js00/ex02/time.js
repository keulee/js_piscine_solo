const month = 7;
let day = 13;

const timeWarp = (newMonth, newDay) => {
  // TODO
  if (month === 7) {
	let month = newMonth;
	console.log(month + " " + newDay);
	// console.log(newDay);
	}
}

console.log(month + " " + day);

timeWarp(5, 10);
timeWarp(11, 20);

