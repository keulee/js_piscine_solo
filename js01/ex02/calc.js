const productInput = document.querySelectorAll('input')[0];
// console.log(product);
const productName = productInput.getAttribute('value');
console.log(productName);

window.onload = function execute() {
};
